<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/5.15.3/css/all.min.css">
    <link rel="stylesheet" href="css/style.css">
</head>
<body>
    <!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
</head>
<body>
    <h1>Todo List: </h1> <!-- Main Heading -->
    <ul class="todoItems"> <!-- Unordered list and assign class to "todoItems" -->
    <% for(let i=0; i < items.length; i++) {%>  <!-- for loop -->
        <li class="item"> <!-- A list with the class of "item" -->
            <% if(items[i].completed === true) {%>
                <span class='completed'><%= items[i].thing %></span>
            <% }else{ %>
                <span><%= items[i].thing %></span>
            <% } %>    
            <span class='fa fa-trash'></span>
        </li>
    <% } %>
    </ul>  <!-- Uses if/else conditional to render added todo list items to html page -->

    <h2>Left to do: <%= left %></h2>

    <h2>Add A Todo:</h2> <!-- Second Heading -->

    <form action="/addTodo" method="POST"> <!-- Form with POST method which goes to /addTodo -->
        <input type="text" placeholder="Thing To Do" name="todoItem">
        <input type="submit">
    </form>
    

    <script src='js/main.js'></script> <!-- Links to main.js file which is in the public folder -->
</body>
</html>
Footer

